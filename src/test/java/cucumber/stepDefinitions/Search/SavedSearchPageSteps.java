package cucumber.stepDefinitions.Search;


public class SavedSearchPageSteps {

   /* @Then("the search should be displayed in saved search")
    public void the_search_should_be_displayed_in_saved_search() {
        SavedSearchesPage savedSearchesPage  = PageFactory.initElements(WebDrv.getInstance().getWebDriver(), SavedSearchesPage.class);
        String jobTitle = VacancyCreationStepsUtil.getJobTitle();
        savedSearchesPage.assertTheSavedSearch(jobTitle);
    }

    @And("I removed the saved search result")
    public void i_removed_the_saved_search_result() {
        SavedSearchesPage savedSearchesPage  = PageFactory.initElements(WebDrv.getInstance().getWebDriver(), SavedSearchesPage.class);
        String jobTitle = VacancyCreationStepsUtil.getJobTitle();
        savedSearchesPage.removeTheSavedSearch(jobTitle);
    }

    @Then("the saved search should not appear in the list")
    public void the_saved_search_should_not_appear_in_the_list() {
        SavedSearchesPage savedSearchesPage  = PageFactory.initElements(WebDrv.getInstance().getWebDriver(), SavedSearchesPage.class);
        String jobTitle = VacancyCreationStepsUtil.getJobTitle();
        savedSearchesPage.assertForRemovedSavedSearch(jobTitle);
    }

    @Then("I can see the job in saved jobs list")
    public void i_can_see_the_job_in_saved_jobs_list() {
        SavedJobsPage savedJobsPage  = PageFactory.initElements(WebDrv.getInstance().getWebDriver(), SavedJobsPage.class);
        String jobTitle = VacancyCreationStepsUtil.getJobTitle();
        savedJobsPage.assertSavedThisJob(jobTitle);
    }

    @And("I can remove the job from saved job list")
    public void i_can_remove_the_job_from() {
        SavedJobsPage savedJobsPage  = PageFactory.initElements(WebDrv.getInstance().getWebDriver(), SavedJobsPage.class);
        String jobTitle = VacancyCreationStepsUtil.getJobTitle();
        savedJobsPage.removeTheSavedJob(jobTitle);
    }

    @Then("the job is removed from the saved list")
    public void the_job_is_removed_from_the_saved_list() {
        SavedJobsPage savedJobsPage  = PageFactory.initElements(WebDrv.getInstance().getWebDriver(), SavedJobsPage.class);
        String jobTitle = VacancyCreationStepsUtil.getJobTitle();
        savedJobsPage.assertForRemovedJob(jobTitle);
    }

    @And("I tap on save search")
    public void i_Tap_On_Save_Search() {
        SavedSearchesPage savedSearchesPage  = PageFactory.initElements(WebDrv.getInstance().getWebDriver(), SavedSearchesPage.class);
        savedSearchesPage.clickSavedSearch();
    }

    @And("I tap the search Continue button")
    public void i_tap_the_search_continue_button() {
        SavedSearchesPage savedSearchesPage  = PageFactory.initElements(WebDrv.getInstance().getWebDriver(), SavedSearchesPage.class);
        savedSearchesPage.clickContinueButton();
    }
*/

}
